export interface _children {
  children: React.ReactNode;
}

export interface SidebarTypes {
  icon: React.ElementType; // Assuming it's a React component like an icon
  text: string;
  isOpen: boolean;
}
